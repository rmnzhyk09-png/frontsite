/*
|--------------------------------------------------------------------------
| Laravel entry point.
|--------------------------------------------------------------------------
| При развёртывании под Laravel увидит тот же самый Vue-бандл,
| что и standalone-сборка (src/). Blade-шаблон подключает этот
| файл через @vite(['resources/js/app.js']).
*/

import '../../src/main.js'